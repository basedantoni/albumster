import Scene from "@/components/scene";
import Albums from "@/components/albums";

const Home = async () => {
  return (
    <div>
      <Scene>
        <Albums />
      </Scene>
    </div>
  );
};

export default Home;
